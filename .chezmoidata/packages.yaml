packages:
  linux:
    pip:
      - pipx
    pipx:
      - package: ansible --include-deps
        dependencies: ansible-lint molecule --include-apps
      - jupyter --include-deps
      - poetry
      - black
      - mypy
      - yq

